- name: 📙 Terminal template - Example
  uses: Shadowghost/gh-metrics@latest
  with:
    template: terminal
    token: MOCKED_TOKEN
    base: header, metadata
    use_mocked_data: yes
    verify: yes
